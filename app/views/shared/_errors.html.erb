<% if obj.errors.any? %>
    <div class="card-panel red lighten-2">
        <span class="white-text">
            <stong> <%= pluralize(obj.errors.count, "error") %>
            prohibited your profile from being created </stong>
            <ul>
                <% obj.errors.full_messages.each do |error|%>
                    <li><%= error%></li>
                <% end %>
            </ul>
        </span>
    </div>
<% end %>
    